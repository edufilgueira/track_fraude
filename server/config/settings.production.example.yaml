app:
  name: track_fraude
  host: 0.0.0.0
  port: 8080
  secret_key: change-me

database:
  backend: postgres
  postgres_url: postgresql://track_fraude:track_fraude@127.0.0.1:5432/track_fraude

auth:
  admin_username: admin
  admin_password: admin123
  admin_display_name: Administrador

pipeline:
  mode: queue

atlas:
  api_url: http://127.0.0.1:8090
  api_key: atlas-dev-internal-key
