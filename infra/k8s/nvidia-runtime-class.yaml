# K3s não define nvidia como runtime default — pods GPU (e o device plugin)
# precisam referenciar esta RuntimeClass explicitamente.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
